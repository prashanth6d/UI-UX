
<link href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,400;0,600;0,700;1,400&display=swap" rel="stylesheet">
<style>
*{box-sizing:border-box;margin:0;padding:0;}
:root{
  --blue:#0099FF;--blue-dark:#0578BE;--blue-bold:#0F3278;
  --orange:#EE743B;--green:#0F9731;--red:#E53935;
  --grey1:#DCDCDC;--grey2:#666666;--grey3:#333333;--grey4:#1D1D1D;
  --surface:#F5F6FA;--header-bg:#EEF2F7;--white:#fff;
  --hover-blue:rgba(0,153,255,0.08);
}
body,html{font-family:'Poppins',sans-serif;}

/* ══════════════════════════════════════
   SHELL
══════════════════════════════════════ */
.shell{display:flex;flex-direction:column;height:100vh;min-width:900px;background:var(--surface);overflow:hidden;}

/* ══════════════════════════════════════
   HEADER
══════════════════════════════════════ */
.hdr{
  display:flex;align-items:center;
  height:72px;min-height:72px;
  background:var(--header-bg);
  border-bottom:1px solid var(--grey1);
  position:relative;z-index:200;flex-shrink:0;
}
/* logo zone */
.hdr-logo{
  width:220px;min-width:220px;height:72px;
  display:flex;align-items:center;padding:0 18px;
  border-right:1px solid var(--grey1);flex-shrink:0;
}
.logo-lockup{display:flex;align-items:center;gap:9px;cursor:pointer;user-select:none;}
.logo-emblem{width:34px;height:34px;flex-shrink:0;}
.logo-text{display:flex;flex-direction:column;line-height:1.25;}
.logo-name{font-size:13px;font-weight:700;color:var(--blue-bold);letter-spacing:-0.2px;white-space:nowrap;}
.logo-tag{font-size:9.5px;color:var(--blue-bold);}
.logo-tag em{font-style:italic;color:var(--orange);}
/* page title */
.hdr-title{flex:1;display:flex;flex-direction:column;justify-content:center;padding:0 24px;min-width:0;}
.hdr-title h1{font-size:24px;font-weight:100;color:var(--grey4);line-height:26px;white-space:nowrap;}
.hdr-title p{font-size:11px;color:var(--grey2);line-height:16px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;}
/* search */
.hdr-search{width:310px;min-width:180px;flex-shrink:0;margin-right:12px;}
.search-pill{
  display:flex;align-items:center;height:40px;border-radius:20px;
  background:linear-gradient(90deg,#7B2FBE,#0099FF);
  padding:0 10px 0 14px;cursor:text;
  transition:filter .18s ease,box-shadow .18s ease;
}
.search-pill:hover{filter:brightness(1.09);}
.search-pill.focused{box-shadow:inset 0 0 0 1.5px rgba(255,255,255,.4);}
.search-pill i.si{font-size:15px;color:rgba(255,255,255,.85);margin-right:9px;flex-shrink:0;}
.search-pill input{flex:1;border:none;outline:none;background:transparent;font-family:'Poppins',sans-serif;font-size:12px;color:#fff;caret-color:#fff;min-width:0;}
.search-pill input::placeholder{color:rgba(255,255,255,.68);}
.mic-btn{width:27px;height:27px;border-radius:50%;background:rgba(255,255,255,.18);display:flex;align-items:center;justify-content:center;flex-shrink:0;margin-left:7px;cursor:pointer;transition:background .15s;}
.mic-btn:hover{background:rgba(255,255,255,.32);}
.mic-btn i{font-size:14px;color:#fff;}
/* right icons */
.hdr-right{display:flex;align-items:center;gap:0;flex-shrink:0;}
.icon-btn{display:flex;flex-direction:column;align-items:center;justify-content:center;gap:3px;padding:6px 12px;border-radius:6px;cursor:pointer;transition:background .15s;position:relative;}
.icon-btn i{font-size:20px;color:#8D8D8D;transition:color .15s;}
.icon-btn span{font-size:9.5px;color:#8D8D8D;white-space:nowrap;transition:color .15s;}
.icon-btn:hover{background:var(--hover-blue);}
.icon-btn:hover i,.icon-btn:hover span{color:var(--blue);}
.notif-dot{position:absolute;top:5px;right:8px;width:7px;height:7px;border-radius:50%;background:var(--red);border:2px solid var(--header-bg);}
/* avatar */
.av-wrap{display:flex;align-items:center;gap:5px;padding:6px 14px 6px 8px;cursor:pointer;border-radius:6px;transition:background .15s;}
.av-wrap:hover{background:var(--hover-blue);}
.av-circle{width:36px;height:36px;border-radius:50%;overflow:hidden;border:2px solid var(--grey1);transition:border-color .15s;flex-shrink:0;background:#C9D6E3;display:flex;align-items:center;justify-content:center;}
.av-wrap:hover .av-circle{border-color:var(--blue);}
.av-chev{font-size:13px;color:#8D8D8D;transition:transform .2s ease,color .15s;}
.av-wrap:hover .av-chev{color:var(--blue);}
.av-wrap.open .av-chev{transform:rotate(180deg);color:var(--blue);}
/* user dropdown */
.user-dd{display:none;position:absolute;top:76px;right:14px;width:196px;background:#fff;border:1px solid var(--grey1);border-radius:8px;box-shadow:0 8px 24px rgba(0,0,0,.12);z-index:9999;}
.user-dd.open{display:block;animation:ddIn .15s ease;}
@keyframes ddIn{from{opacity:0;transform:translateY(-5px);}to{opacity:1;transform:translateY(0);}}
.dd-head{display:flex;align-items:center;gap:10px;padding:12px 14px;border-bottom:1px solid var(--grey1);}
.dd-av{width:34px;height:34px;border-radius:50%;background:#C9D6E3;display:flex;align-items:center;justify-content:center;font-size:12px;font-weight:600;color:#0578BE;flex-shrink:0;}
.dd-n{font-size:12px;font-weight:600;color:var(--grey4);}
.dd-r{font-size:10px;color:var(--grey2);margin-top:1px;}
.dd-item{display:flex;align-items:center;gap:9px;height:38px;padding:0 14px;cursor:pointer;transition:background .12s;}
.dd-item i{font-size:15px;color:#666;transition:color .12s;}
.dd-item s2{font-size:12px;color:var(--grey3);transition:color .12s;}
.dd-item:hover{background:#F0F4FF;}
.dd-item:hover i,.dd-item:hover s2{color:var(--blue);}
.dd-hr{border:none;border-top:1px solid var(--grey1);margin:3px 0;}
.dd-item.dng i,.dd-item.dng s2{color:var(--red);}
.dd-item.dng:hover{background:#FDECEA;}
.dd-item.dng:hover i,.dd-item.dng:hover s2{color:var(--red);}

/* ══════════════════════════════════════
   BODY ROW (sidebar + content)
══════════════════════════════════════ */
.body-row{display:flex;flex:1;overflow:hidden;}

/* ══════════════════════════════════════
   SIDEBAR
══════════════════════════════════════ */
.sidebar{
  width:220px;min-width:220px;background:#fff;
  border-right:1px solid var(--grey1);
  display:flex;flex-direction:column;
  overflow:hidden;flex-shrink:0;
}
.nav-search{display:flex;align-items:center;gap:9px;height:44px;padding:0 14px;background:#F5F6FA;border-bottom:1px solid var(--grey1);cursor:pointer;flex-shrink:0;}
.nav-search:hover{background:#EBEBF0;}
.nav-search i{font-size:15px;color:#8D8D8D;}
.nav-search span{font-size:12px;color:#8D8D8D;flex:1;}
.nav-search .kbd{border:1px solid var(--grey1);border-radius:3px;padding:1px 5px;font-size:10px;color:#666;background:#fff;}
.nav-body{flex:1;overflow-y:auto;overflow-x:hidden;padding:4px 0;}
.nav-body::-webkit-scrollbar{width:3px;}
.nav-body::-webkit-scrollbar-thumb{background:var(--grey1);border-radius:2px;}
/* nav item */
.ni{display:flex;align-items:center;gap:11px;height:46px;padding:0 14px;cursor:pointer;position:relative;transition:background .14s;}
.ni i.nico{font-size:19px;color:#8D8D8D;flex-shrink:0;transition:color .14s;}
.ni .nlabel{font-size:12.5px;color:var(--grey4);flex:1;transition:color .14s;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;}
.ni .nchev{font-size:13px;color:#8D8D8D;transition:color .14s,transform .24s ease;flex-shrink:0;}
.ni::before{content:'';position:absolute;left:0;top:0;bottom:0;width:3px;background:transparent;border-radius:0 2px 2px 0;transition:background .14s;}
.ni:hover{background:#F0F4FF;}
.ni:hover i.nico,.ni:hover .nlabel,.ni:hover .nchev{color:var(--blue);}
.ni.active{background:#F0F4FF;}
.ni.active::before{background:var(--blue);}
.ni.active i.nico,.ni.active .nlabel{color:var(--blue);}
.ni.active .nlabel{font-weight:600;}
.ni.grp-open{background:#EBF5FF;}
.ni.grp-open::before{background:var(--blue);}
.ni.grp-open i.nico,.ni.grp-open .nlabel,.ni.grp-open .nchev{color:var(--blue);}
.ni.grp-open .nlabel{font-weight:600;}
.ni.grp-open .nchev{transform:rotate(180deg);}
.ni.disabled{cursor:not-allowed;}
.ni.disabled:hover{background:transparent;}
.ni.disabled:hover i.nico,.ni.disabled:hover .nlabel{color:inherit;}
.ni.disabled i.nico{color:#DCDCDC;}
.ni.disabled .nlabel{color:#BDBDBD;}
/* sub group */
.sub-grp{overflow:hidden;max-height:0;transition:max-height .24s ease;background:#F5F6FA;}
.sub-grp.open{max-height:220px;}
.si2{display:flex;align-items:center;height:38px;padding:0 14px 0 44px;cursor:pointer;transition:background .14s;}
.si2 .slabel{font-size:12px;color:#666;transition:color .14s;}
.si2:hover{background:#EBF5FF;}
.si2:hover .slabel{color:var(--blue-dark);}
.si2.active .slabel{color:var(--orange);font-weight:600;}
/* bottom */
.nav-bottom{border-top:1px solid var(--grey1);flex-shrink:0;}

/* ══════════════════════════════════════
   MAIN CONTENT
══════════════════════════════════════ */
.main{flex:1;display:flex;flex-direction:column;overflow:hidden;}
.main-inner{flex:1;overflow-y:auto;overflow-x:hidden;padding:20px;}
.main-inner::-webkit-scrollbar{width:4px;}
.main-inner::-webkit-scrollbar-thumb{background:var(--grey1);border-radius:2px;}

/* ── TOOLBAR ── */
.toolbar{display:flex;align-items:center;gap:8px;margin-bottom:14px;flex-wrap:wrap;}
.tlabel{font-size:13px;color:var(--grey4);margin-right:4px;white-space:nowrap;}
.tlabel b{font-weight:700;}
.tb-inp{display:flex;align-items:center;gap:6px;height:34px;border:1px solid var(--grey1);border-radius:4px;background:#fff;padding:0 10px;min-width:160px;}
.tb-inp input{border:none;outline:none;font-family:'Poppins',sans-serif;font-size:11.5px;color:var(--grey4);background:transparent;width:100%;}
.tb-inp input::placeholder{color:#8D8D8D;}
.tb-inp i{font-size:13px;color:#8D8D8D;flex-shrink:0;}
.tbd{position:relative;display:inline-block;}
.tb-btn{display:flex;align-items:center;gap:5px;height:34px;border:1px solid var(--grey1);border-radius:4px;background:#fff;padding:0 11px;font-family:'Poppins',sans-serif;font-size:11.5px;color:var(--grey4);cursor:pointer;white-space:nowrap;}
.tb-btn i{font-size:12px;color:#666;}
.tb-btn:hover{background:#F0F4FF;}
.dpanel{display:none;position:absolute;top:38px;left:0;background:#fff;border:1px solid var(--grey1);border-radius:4px;box-shadow:0 4px 12px rgba(0,0,0,.10);min-width:160px;z-index:500;padding:4px 0;}
.dpanel.open{display:block;}
.ditem{padding:7px 13px;font-size:11.5px;color:var(--grey4);cursor:pointer;}
.ditem:hover{background:#F0F4FF;}
.ditem.sel{font-weight:600;color:var(--blue);}

/* ── TABLE CARD ── */
.tcard{background:#fff;border:1px solid var(--grey1);box-shadow:0 2px 8px rgba(0,0,0,.07);border-radius: 4px; overflow: hidden;}
table{width:100%;border-collapse:collapse;table-layout:fixed;}
thead tr{background:#F5F5F5;height:45px;border-bottom:1px solid var(--grey1);}
th{padding:0 16px;font-size:11.5px;font-weight:600;color:var(--grey4);text-align:left;white-space:nowrap;vertical-align:middle;user-select:none;cursor:pointer;}
th:hover{background:#EBEBEB;}
th.cb-th{width:36px;padding:0;text-align:center;cursor:default;}
th.cb-th:hover{background:#F5F5F5;}
th .sico{font-size:10px;color:#888;margin-left:3px;}
th.sort-asc .sico::before{content:"↑";}
th.sort-desc .sico::before{content:"↓";}
th.sort-asc .sico,th.sort-desc .sico{color:var(--blue);}
/* col widths */
.c1{width:36px;}.c2{width:148px;}.c3{width:140px;}.c4{width:175px;}.c5{width:145px;}.c6{width:160px;}.c7{width:68px;}.c8{width:105px;}.c9{width:140px;}.c10{width:52px;}
tbody tr{border-bottom:1px solid var(--grey1);transition:background .1s;}
tbody tr:last-child{border-bottom:none;}
tbody tr:hover{background:#F0F4FF;}
tbody tr.row-sel{background:#EBF5FF;}
td{padding:12px 16px;font-size:11.5px;color:var(--grey4);vertical-align:middle;}
td.cb-td{padding:0;text-align:center;}
input[type=checkbox]{width:14px;height:14px;accent-color:var(--blue);cursor:pointer;border-radius:2px;}
/* msisdn */
.mslink{display:flex;align-items:center;gap:3px;color:var(--blue);font-weight:600;font-size:11.5px;cursor:pointer;}
.mslink i{font-size:11px;}
.msdate{font-size:10.5px;color:#666;margin-top:2px;}
/* status pill */
.sw{display:flex;align-items:center;gap:5px;}
.spill{display:inline-flex;align-items:center;gap:5px;height:26px;border-radius:6px;padding:0 9px;font-size:11px;font-weight:600;border:1px solid;cursor:pointer;white-space:nowrap;transition:filter .15s;}
.spill:hover{filter:brightness(.95);}
.spill i{font-size:9px;}
.s-a{background:#E6F4EA;color:#0F9731;border-color:#0F9731;}
.s-su{background:#EBF5FF;color:#0578BE;border-color:#0578BE;}
.s-te{background:#FDECEA;color:var(--red);border-color:var(--red);}
.s-de{background:#FFF3E0;color:var(--orange);border-color:var(--orange);}
.s-it{background:#F5F5F5;color:#666;border-color:var(--grey1);}
.rdot{width:6px;height:6px;background:var(--red);border-radius:50%;flex-shrink:0;}
/* iccid */
.iline{display:block;font-size:11.5px;color:var(--grey4);line-height:17px;}
/* tags */
.vlink{color:var(--blue);font-weight:600;font-size:11.5px;cursor:pointer;}
.vlink:hover{text-decoration:underline;}
/* ai text */
.ait{overflow:hidden;text-overflow:ellipsis;white-space:nowrap;font-size:11.5px;}
/* actions */
.act-cell{text-align:center;position:relative;}
.dots{position:relative;width:26px;height:26px;display:inline-flex;align-items:center;justify-content:center;border:none;background:transparent;cursor:pointer;border-radius:4px;font-size:15px;color:#666;transition:background .12s;}
.dots:hover{background:#F0F4FF;}
.rdot-btn{position:absolute;top:1px;right:0;width:6px;height:6px;background:var(--red);border-radius:50%;pointer-events:none;}
/* context menu */
.ctx{display:none;position:absolute;right:32px;top:50%;transform:translateY(-50%);background:#fff;border:1px solid var(--grey1);border-radius:4px;box-shadow:0 4px 16px rgba(0,0,0,.12);min-width:172px;z-index:600;padding:4px 0;}
.ctx.open{display:block;animation:ddIn .12s ease;}
.cxi{display:flex;align-items:center;gap:8px;height:35px;padding:0 13px;font-size:11.5px;color:var(--grey3);cursor:pointer;transition:background .1s;}
.cxi i{font-size:13px;color:#666;transition:color .1s;}
.cxi:hover{background:#F0F4FF;}
.cxi:hover i{color:var(--blue);}
.cxi.dng{color:var(--red);}
.cxi.dng i{color:var(--red);}
.cxi.dng:hover{background:#FDECEA;}
.ctx-hr{border:none;border-top:1px solid var(--grey1);margin:3px 0;}
/* pagination */
.pgbar{display:flex;align-items:center;justify-content:space-between;padding:0 16px;height:42px;border-top:1px solid var(--grey1);}
.pvsel{height:26px;border:1px solid var(--grey1);border-radius:4px;font-family:'Poppins',sans-serif;font-size:11.5px;color:var(--grey4);padding:0 5px;cursor:pointer;outline:none;}
.pvlbl{font-size:11.5px;color:#666;margin-left:6px;}
.pgnums{display:flex;align-items:center;gap:3px;}
.pgb{width:26px;height:26px;border:1px solid var(--grey1);border-radius:4px;display:inline-flex;align-items:center;justify-content:center;font-size:11.5px;color:var(--grey3);cursor:pointer;background:#fff;transition:background .12s;}
.pgb:hover{background:#F0F4FF;}
.pgb.act{background:var(--blue);color:#fff;border-color:var(--blue);}
.pgdot{font-size:11.5px;color:#666;padding:0 3px;}

/* overlay for dropdowns */
.overlay{display:none;position:fixed;inset:0;z-index:400;}
.overlay.on{display:block;}
</style>

<!-- DROPDOWN CLOSE OVERLAY -->
<div class="overlay" id="overlay" onclick="closeAll()"></div>

<div class="shell">

  <!-- ══ HEADER ══ -->
  <header class="hdr">
    <div class="hdr-logo">
      <div class="logo-lockup">
        <svg class="logo-emblem" viewBox="0 0 34 34" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
          <circle cx="17" cy="17" r="15" stroke="#0099FF" stroke-width="2.8" fill="none"/>
          <path d="M9.5 24.5 A10.5 10.5 0 0 1 9.5 9.5" stroke="#0578BE" stroke-width="3.2" stroke-linecap="round" fill="none"/>
          <rect x="13" y="11" width="7" height="12" rx="3.5" fill="url(#pg)"/>
          <defs><linearGradient id="pg" x1="13" y1="11" x2="20" y2="23" gradientUnits="userSpaceOnUse"><stop offset="0%" stop-color="#EE743B"/><stop offset="100%" stop-color="#F7D21E"/></linearGradient></defs>
        </svg>
        <div class="logo-text">
          <span class="logo-name">6D Technologies</span>
          <span class="logo-tag">Smart <em>Ideas,</em> Delivered</span>
        </div>
      </div>
    </div>

    <div class="hdr-title">
      <h1 id="pageTitle">SIM Management</h1>
      <p id="pageSubtitle">Activate, monitor, and configure SIMs with full lifecycle controls.</p>
    </div>

    <div class="hdr-search">
      <div class="search-pill" id="sPill" onclick="document.getElementById('sInput').focus()">
        <i class="ti ti-search si" aria-hidden="true"></i>
        <input type="text" id="sInput" placeholder="Type to search or press '/' to begin" aria-label="AARYA search">
        <div class="mic-btn" title="Voice search" onclick="event.stopPropagation()">
          <i class="ti ti-microphone" aria-hidden="true"></i>
        </div>
      </div>
    </div>

    <div class="hdr-right">
      <div class="icon-btn" title="Tagged Entities">
        <i class="ti ti-bookmark" aria-hidden="true"></i>
        <span>Tagged Entity</span>
      </div>
      <div class="icon-btn" title="Reminders">
        <i class="ti ti-bell" aria-hidden="true"></i>
        <span>Reminders</span>
        <span class="notif-dot" aria-label="New notifications"></span>
      </div>
    </div>

    <div class="av-wrap" id="avWrap" onclick="toggleDD()">
      <div class="av-circle" aria-label="User profile">
        <svg viewBox="0 0 36 36" width="36" height="36"><circle cx="18" cy="18" r="18" fill="#C9D6E3"/><circle cx="18" cy="14" r="6.5" fill="#8DAFC8"/><ellipse cx="18" cy="30" rx="11" ry="8" fill="#8DAFC8"/></svg>
      </div>
      <i class="ti ti-chevron-down av-chev" aria-hidden="true"></i>
    </div>

    <div class="user-dd" id="userDD">
      <div class="dd-head">
        <div class="dd-av">AR</div>
        <div><div class="dd-n">Arya Patel</div><div class="dd-r">Administrator</div></div>
      </div>
      <div class="dd-item"><i class="ti ti-user" aria-hidden="true"></i><s2>My Profile</s2></div>
      <div class="dd-item"><i class="ti ti-settings" aria-hidden="true"></i><s2>Settings</s2></div>
      <div class="dd-item"><i class="ti ti-lock" aria-hidden="true"></i><s2>Change Password</s2></div>
      <hr class="dd-hr">
      <div class="dd-item dng"><i class="ti ti-logout" aria-hidden="true"></i><s2>Logout</s2></div>
    </div>
  </header>

  <!-- ══ BODY ══ -->
  <div class="body-row">

    <!-- ── SIDEBAR ── -->
    <nav class="sidebar" aria-label="Main navigation">
      <div class="nav-search" onclick="document.getElementById('sInput').focus()">
        <i class="ti ti-search" aria-hidden="true"></i>
        <span>Search menu</span>
        <span class="kbd">/</span>
      </div>
      <div class="nav-body">
        <div class="ni" onclick="navGo(this,'Dashboard & Ai Insights','Monitor your IoT fleet with AI-powered analytics and insights.')" data-id="dash">
          <i class="ti ti-layout-dashboard nico" aria-hidden="true"></i>
          <span class="nlabel">Dashboard & Ai Insights</span>
        </div>
        <div class="ni" onclick="navGo(this,'My Account','Manage your account settings, profile and preferences.')" data-id="acct">
          <i class="ti ti-user-cog nico" aria-hidden="true"></i>
          <span class="nlabel">My Account</span>
        </div>
        <!-- GROUP: Manage SIMs -->
        <div class="ni grp-open" id="g-sims" onclick="toggleGrp('sg-sims','g-sims')" aria-expanded="true">
          <i class="ti ti-device-sim-card nico" aria-hidden="true"></i>
          <span class="nlabel">Manage SIMs</span>
          <i class="ti ti-chevron-down nchev" aria-hidden="true"></i>
        </div>
        <div class="sub-grp open" id="sg-sims">
          <div class="si2 active" onclick="subNav(this,'g-sims','SIM Management','Activate, monitor, and configure SIMs with full lifecycle controls.')"><span class="slabel">SIMs</span></div>
          <div class="si2" onclick="subNav(this,'g-sims','SIM Groups','Manage and organise your SIMs into logical groups.')"><span class="slabel">SIM Groups</span></div>
          <div class="si2" onclick="subNav(this,'g-sims','SIM Tags','Assign and manage tags across your SIM inventory.')"><span class="slabel">SIM Tags</span></div>
        </div>
        <div class="ni disabled" data-id="users">
          <i class="ti ti-circle nico" aria-hidden="true"></i>
          <span class="nlabel">Users & Roles</span>
        </div>
        <div class="ni" onclick="navGo(this,'Billing & Invoicing','View invoices, payment history and subscription details.')" data-id="bill">
          <i class="ti ti-file-invoice nico" aria-hidden="true"></i>
          <span class="nlabel">Billing & Invoicing</span>
        </div>
        <div class="ni" onclick="navGo(this,'Analytics & Reporting','Deep-dive analytics and exportable reports for your fleet.')" data-id="anal">
          <i class="ti ti-chart-bar nico" aria-hidden="true"></i>
          <span class="nlabel">Analytics & Reporting</span>
        </div>
        <!-- GROUP: Operations -->
        <div class="ni" id="g-ops" onclick="toggleGrp('sg-ops','g-ops')" aria-expanded="true">
          <i class="ti ti-settings nico" aria-hidden="true"></i>
          <span class="nlabel">Operations & Support</span>
          <i class="ti ti-chevron-down nchev" aria-hidden="true"></i>
        </div>
        <div class="sub-grp" id="sg-ops">
          <div class="si2" onclick="subNav(this,'g-ops','Diagnose & Troubleshoot','Run diagnostics and troubleshoot SIM connectivity issues.')"><span class="slabel">Diagnose & Troubleshoot</span></div>
          <div class="si2" onclick="subNav(this,'g-ops','Tickets & Support','Raise and track support tickets for your fleet.')"><span class="slabel">Tickets & Support</span></div>
          <div class="si2" onclick="subNav(this,'g-ops','Rule Automation','Create automation rules to manage SIM lifecycle events.')"><span class="slabel">Rule Automation</span></div>
          <div class="si2" onclick="subNav(this,'g-ops','Message Templates','Design and manage SMS/notification templates.')"><span class="slabel">Message Templates</span></div>
        </div>
      </div>
      <div class="nav-bottom">
        <div class="ni" onclick="navGo(this,'User Guides & FAQs','Browse documentation, guides and frequently asked questions.')" data-id="help">
          <i class="ti ti-help-square nico" aria-hidden="true"></i>
          <span class="nlabel">User Guides & FAQs</span>
        </div>
      </div>
    </nav>

    <!-- ── MAIN CONTENT ── -->
    <div class="main">
      <div class="main-inner">

        <!-- TOOLBAR -->
        <div class="toolbar">
          <span class="tlabel">Listing <b id="listCount">500</b> SIMs</span>
          <div class="tb-inp">
            <i class="ti ti-search" aria-hidden="true"></i>
            <input type="text" placeholder="Search by MSISDN" id="tableSearch" oninput="filterTable()">
          </div>
          <div class="tbd">
            <button class="tb-btn" onclick="openDrop('dd-col')" aria-haspopup="true">MSISDN <i class="ti ti-chevron-down" aria-hidden="true"></i></button>
            <div class="dpanel" id="dd-col">
              <div class="ditem sel">MSISDN</div>
              <div class="ditem" onclick="selectItem(this)">ICCID</div>
              <div class="ditem" onclick="selectItem(this)">IMSI</div>
              <div class="ditem" onclick="selectItem(this)">Account ID</div>
            </div>
          </div>
          <div class="tbd">
            <button class="tb-btn" onclick="openDrop('dd-st')" aria-haspopup="true">All Status (500) <i class="ti ti-chevron-down" aria-hidden="true"></i></button>
            <div class="dpanel" id="dd-st">
              <div class="ditem sel" onclick="filterStatus('',this)">All Status (500)</div>
              <div class="ditem" onclick="filterStatus('Active',this)">Active (312)</div>
              <div class="ditem" onclick="filterStatus('Suspended',this)">Suspended (44)</div>
              <div class="ditem" onclick="filterStatus('Terminated',this)">Terminated (28)</div>
              <div class="ditem" onclick="filterStatus('Deactivated',this)">Deactivated (67)</div>
              <div class="ditem" onclick="filterStatus('In Test',this)">In Test (49)</div>
            </div>
          </div>
          <div class="tbd">
            <button class="tb-btn" onclick="openDrop('dd-flt')"><i class="ti ti-filter" aria-hidden="true"></i> More Filters <i class="ti ti-chevron-down" aria-hidden="true"></i></button>
            <div class="dpanel" id="dd-flt">
              <div class="ditem" onclick="selectItem(this)">Date Modified</div>
              <div class="ditem" onclick="selectItem(this)">Date Created</div>
              <div class="ditem" onclick="selectItem(this)">Group</div>
              <div class="ditem" onclick="selectItem(this)">Plan Name</div>
            </div>
          </div>
        </div>

        <!-- TABLE -->
        <div class="tcard">
          <table>
            <thead>
              <tr>
                <th class="cb-th c1"><input type="checkbox" id="selAll" onchange="toggleAll(this)" aria-label="Select all"></th>
                <th class="c2" onclick="sortTable(1)">MSISDN <span class="sico"></span></th>
                <th class="c3" onclick="sortTable(2)">Status <span class="sico"></span></th>
                <th class="c4" onclick="sortTable(3)">ICCID / IMSI <span class="sico"></span></th>
                <th class="c5" onclick="sortTable(4)">Account ID <span class="sico"></span></th>
                <th class="c6" onclick="sortTable(5)">Plan Name <span class="sico"></span></th>
                <th class="c7">Tags</th>
                <th class="c8" onclick="sortTable(7)">Group <span class="sico"></span></th>
                <th class="c9" onclick="sortTable(8)">Ai Insights <span class="sico"></span></th>
                <th class="c10">Actions</th>
              </tr>
            </thead>
            <tbody id="tBody"></tbody>
          </table>
          <div class="pgbar">
            <div style="display:flex;align-items:center;">
              <select class="pvsel" aria-label="Rows per page" onchange="changePerPage(this.value)"><option>10</option><option>25</option><option>50</option></select>
              <span class="pvlbl">Per View</span>
            </div>
            <div class="pgnums" id="pgNums"></div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<script>
// ─── DATA ───────────────────────────────────────────────────────
const STATUSES=['Active','Active','Active','Suspended','Terminated','Active','Deactivated','Active','In Test','Active'];
const PLANS=['IoT Basic Connect','M2M 100MB 4G DYNAMI…','M2M 100MB 4G DYNAMI…','M2M 100MB 4G DYNAMI…','M2M 100MB 4G DYNAMI…','M2M 100MB 4G DYNAMI…','M2M 100MB 4G DYNAMI…','Enterprise 5G UNLIM…','IoT Basic Connect','M2M 100MB 4G DYNAMI…'];
const AI_TEXT=['4x usage increase this month','Stable — no anomalies detected','Unusual data spike detected','No activity in 45 days','No activity in 90+ days','Plan upgrade recommended','Low usage — consider downgrade','Peak usage pattern detected','Test phase nominal performance','Roaming charges increasing'];
const GROUPS=['GroupAlpha','GroupBeta','GroupAlpha','GroupGamma','GroupBeta','GroupAlpha','GroupGamma','GroupBeta','GroupAlpha','GroupBeta'];
const ALERT_ROWS=[0,3]; // rows with red alert dots

let allRows=[];
let filtered=[];
let currentPage=1;
let perPage=10;
let sortCol=-1,sortDir=1;
let statusFilter='';
let searchFilter='';

// Generate 50 rows
for(let i=0;i<50;i++){
  allRows.push({
    msisdn:'728362'+(3526+i),
    date:'0'+((i%9)+1)+'/03/2025',
    status:STATUSES[i%10],
    iccid:'134786153'+(4314+i),
    imsi:'237465134'+(7632+i),
    acct:'AC-12345678'+(90+i)%100,
    plan:PLANS[i%10],
    group:GROUPS[i%10],
    ai:AI_TEXT[i%10],
    alert:ALERT_ROWS.includes(i%10)
  });
}
filtered=[...allRows];

// ─── RENDER TABLE ─────────────────────────────────────────────
function renderTable(){
  const tbody=document.getElementById('tBody');
  const start=(currentPage-1)*perPage;
  const rows=filtered.slice(start,start+perPage);
  tbody.innerHTML=rows.map((r,idx)=>{
    const sc=statusClass(r.status);
    const alertPill=r.alert?'<span class="rdot"></span>':'';
    const alertBtn=r.alert?'<span class="rdot-btn"></span>':'';
    return`<tr id="row-${start+idx}" onclick="rowClick(${start+idx},event)">
      <td class="cb-td"><input type="checkbox" class="rowcb" data-idx="${start+idx}" onchange="rowCheck(this)" onclick="event.stopPropagation()" aria-label="Select row"></td>
      <td><div class="mslink">${r.msisdn} <i class="ti ti-external-link" aria-hidden="true"></i></div><div class="msdate">${r.date}</div></td>
      <td><div class="sw"><span class="spill ${sc}">${r.status} <i class="ti ti-chevron-down" aria-hidden="true"></i></span>${alertPill}</div></td>
      <td><span class="iline">${r.iccid}</span><span class="iline">${r.imsi}</span></td>
      <td>${r.acct}</td>
      <td style="overflow:hidden;text-overflow:ellipsis;white-space:nowrap;">${r.plan}${r.alert?' <span class="rdot" style="display:inline-block;vertical-align:middle;margin-left:3px;"></span>':''}</td>
      <td><a class="vlink">View</a></td>
      <td style="white-space:nowrap;overflow:hidden;text-overflow:ellipsis;">${r.group}</td>
      <td><div class="ait" title="${r.ai}">${r.ai}</div></td>
      <td class="act-cell">
        <button class="dots" onclick="openCtx(this,event)" aria-label="Row actions">⋮${alertBtn}</button>
        <div class="ctx">
          <div class="cxi"><i class="ti ti-eye" aria-hidden="true"></i>View Details</div>
          <div class="cxi"><i class="ti ti-edit" aria-hidden="true"></i>Edit SIM</div>
          <div class="cxi"><i class="ti ti-transfer" aria-hidden="true"></i>Change Plan</div>
          <div class="cxi"><i class="ti ti-pause" aria-hidden="true"></i>Suspend</div>
          <hr class="ctx-hr">
          <div class="cxi dng"><i class="ti ti-circle-x" aria-hidden="true"></i>Request Termination</div>
        </div>
      </td>
    </tr>`;
  }).join('');
  renderPager();
  document.getElementById('listCount').textContent=filtered.length;
}

function statusClass(s){
  return s==='Active'?'s-a':s==='Suspended'?'s-su':s==='Terminated'?'s-te':s==='Deactivated'?'s-de':'s-it';
}

// ─── PAGINATION ───────────────────────────────────────────────
function renderPager(){
  const total=Math.ceil(filtered.length/perPage);
  const pg=document.getElementById('pgNums');
  let html='';
  const pages=[1,2];
  if(currentPage>3&&currentPage<total-1)pages.push(currentPage);
  pages.push(total);
  const uniq=[...new Set(pages)].filter(p=>p>=1&&p<=total).sort((a,b)=>a-b);
  let prev=0;
  uniq.forEach(p=>{
    if(prev&&p-prev>1)html+=`<span class="pgdot">…</span>`;
    html+=`<button class="pgb${p===currentPage?' act':''}" onclick="goPage(${p})">${p}</button>`;
    prev=p;
  });
  html+=`<button class="pgb" onclick="goPage(${Math.min(currentPage+1,total)})" aria-label="Next page"><i class="ti ti-chevron-right" aria-hidden="true"></i></button>`;
  pg.innerHTML=html;
}
function goPage(p){currentPage=p;renderTable();}
function changePerPage(v){perPage=+v;currentPage=1;renderTable();}

// ─── FILTER / SORT ────────────────────────────────────────────
function applyFilters(){
  filtered=allRows.filter(r=>{
    const matchS=!statusFilter||r.status===statusFilter;
    const matchQ=!searchFilter||r.msisdn.includes(searchFilter)||r.acct.toLowerCase().includes(searchFilter.toLowerCase());
    return matchS&&matchQ;
  });
  if(sortCol>=0) doSort();
  currentPage=1;
  renderTable();
}
function filterTable(){searchFilter=document.getElementById('tableSearch').value;applyFilters();}
function filterStatus(s,el){
  statusFilter=s;
  document.querySelectorAll('#dd-st .ditem').forEach(d=>d.classList.remove('sel'));
  el.classList.add('sel');
  closeAll();
  applyFilters();
}
function sortTable(col){
  const ths=document.querySelectorAll('thead th');
  if(sortCol===col)sortDir*=-1; else{sortDir=1;sortCol=col;}
  ths.forEach(t=>{t.classList.remove('sort-asc','sort-desc');});
  ths[col].classList.add(sortDir===1?'sort-asc':'sort-desc');
  doSort();renderTable();
}
function doSort(){
  const keys=['','msisdn','status','iccid','acct','plan','','group','ai'];
  const key=keys[sortCol];
  if(!key)return;
  filtered.sort((a,b)=>a[key].localeCompare(b[key])*sortDir);
}

// ─── SELECT ───────────────────────────────────────────────────
function toggleAll(cb){document.querySelectorAll('.rowcb').forEach(c=>{c.checked=cb.checked;const tr=c.closest('tr');tr&&(cb.checked?tr.classList.add('row-sel'):tr.classList.remove('row-sel'));});}
function rowCheck(cb){const tr=cb.closest('tr');tr&&(cb.checked?tr.classList.add('row-sel'):tr.classList.remove('row-sel'));}
function rowClick(idx,e){if(e.target.tagName==='INPUT'||e.target.tagName==='BUTTON'||e.target.closest('.dots')||e.target.closest('.ctx'))return;const cb=document.querySelector(`[data-idx="${idx}"]`);if(cb){cb.checked=!cb.checked;rowCheck(cb);}}

// ─── DROPDOWNS ────────────────────────────────────────────────
function openDrop(id){
  const el=document.getElementById(id);
  const isOpen=el.classList.contains('open');
  closeAll();
  if(!isOpen){el.classList.add('open');document.getElementById('overlay').classList.add('on');}
}
function openCtx(btn,e){
  e.stopPropagation();
  const ctx=btn.nextElementSibling;
  const isOpen=ctx.classList.contains('open');
  closeAll();
  if(!isOpen){ctx.classList.add('open');document.getElementById('overlay').classList.add('on');}
}
function toggleDD(){
  const dd=document.getElementById('userDD');
  const aw=document.getElementById('avWrap');
  const isOpen=dd.classList.contains('open');
  closeAll();
  if(!isOpen){dd.classList.add('open');aw.classList.add('open');document.getElementById('overlay').classList.add('on');}
}
function closeAll(){
  document.querySelectorAll('.dpanel.open,.ctx.open').forEach(e=>e.classList.remove('open'));
  document.getElementById('userDD').classList.remove('open');
  document.getElementById('avWrap').classList.remove('open');
  document.getElementById('overlay').classList.remove('on');
}
function selectItem(el){el.closest('.dpanel').querySelectorAll('.ditem').forEach(d=>d.classList.remove('sel'));el.classList.add('sel');closeAll();}

// ─── SEARCH FOCUS ─────────────────────────────────────────────
const sInput=document.getElementById('sInput');
const sPill=document.getElementById('sPill');
sInput.addEventListener('focus',()=>sPill.classList.add('focused'));
sInput.addEventListener('blur',()=>sPill.classList.remove('focused'));
document.addEventListener('keydown',e=>{
  if(e.key==='/'&&document.activeElement!==sInput&&document.activeElement!==document.getElementById('tableSearch')){
    e.preventDefault();sInput.focus();
  }
  if(e.key==='Escape'){sInput.blur();closeAll();}
});

// ─── NAV FUNCTIONS ────────────────────────────────────────────
function navGo(el,title,sub){
  document.querySelectorAll('.ni.active,.si2.active').forEach(n=>n.classList.remove('active'));
  el.classList.add('active');
  document.getElementById('pageTitle').textContent=title;
  document.getElementById('pageSubtitle').textContent=sub;
}
function subNav(el,grpId,title,sub){
  document.querySelectorAll('.ni.active,.si2.active').forEach(n=>n.classList.remove('active'));
  const grp=document.getElementById(grpId);
  if(grp)grp.classList.add('active');
  el.classList.add('active');
  document.getElementById('pageTitle').textContent=title;
  document.getElementById('pageSubtitle').textContent=sub;
}
function toggleGrp(sgId,gId){
  const sg=document.getElementById(sgId);
  const gi=document.getElementById(gId);
  const isOpen=sg.classList.contains('open');
  sg.classList.toggle('open',!isOpen);
  gi.classList.toggle('grp-open',!isOpen);
  gi.setAttribute('aria-expanded',String(!isOpen));
}

// ─── INIT ─────────────────────────────────────────────────────
renderTable();
</script>
